services:
  api:
    build: .
    network_mode: host
    environment:
      NODE_ENV: production
      USE_HTTP: "1"
      DATABASE_HOST: 127.0.0.1
      DATABASE_PORT: "3306"
      DATABASE_NAME: tucarrera2026
      DATABASE_USER: tcuser
      DATABASE_PASSWORD: TC_abc123#
    restart: unless-stopped
